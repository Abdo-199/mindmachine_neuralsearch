version: '3'
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    tty: true
    restart: always
  
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ../backend-vol:/usr/src/app/data
      - ../mm_docs:/usr/src/app/mm_docs
    tty: true
    restart: always
